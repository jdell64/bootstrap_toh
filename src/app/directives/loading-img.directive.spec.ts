import { LoadingImgDirective } from './loading-img.directive';

describe('LoadingImgDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingImgDirective();
    expect(directive).toBeTruthy();
  });
});
